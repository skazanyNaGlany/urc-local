// // use anyhow::Result;

// use std::fs::read_to_string;
// use std::env;


// mod utils {
//     pub(crate) fn get_exe_name() -> Result<String> {
//         let exe_path = std::env::current_exe()?;

//         return exe_path.file_name().;
//     }
// }
